server:
  port: 8080

spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4
          temperature: 0.5

logging:
  level:
    com.taskgenerator: INFO
    org.springframework.ai: INFO

