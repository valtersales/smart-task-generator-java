server:
  port: 8080

spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-3.5-turbo
          temperature: 0.7

logging:
  level:
    com.taskgenerator: DEBUG
    org.springframework.ai: DEBUG

